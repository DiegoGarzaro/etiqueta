# Copy to .env and adjust as needed.
INV_APP_NAME="Inventário Doméstico"
# SQLite (zero-config, default). Swap for Postgres in production:
# INV_DATABASE_URL="postgresql+asyncpg://user:pass@localhost:5432/inventory"
INV_DATABASE_URL="sqlite+aiosqlite:///./inventory.db"
INV_CORS_ORIGINS='["http://localhost:5173"]'
